apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: sts-storage
spec:
  serviceName: svc-storage
  replicas: 1
  selector:
    matchLabels:
      app: storage
  template:
    metadata:
      labels:
        app: storage
    spec:
      containers:
        - name: storage
          image: gcr.io/examen-td-494215/backend-storage:latest
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: storage-data
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: storage-data
      spec:
        accessModes: ReadWriteOnce
        storageClassName: standard
        resources:
          requests:
            storage: 1Gi
